import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
// it the acess process the into the data into js
//{ /* {comments.map((comment) => {
  //return <div key={comment.id} className='shadow-lg text-red-700 p-4 space-y-2' border-8 border-indigo-600>
  //<h1 className='text-purple-500'>{comment.name}</h1>
 // <p>{comment.email}</p>
  //<p>{comment.body}</p>
//</div>



